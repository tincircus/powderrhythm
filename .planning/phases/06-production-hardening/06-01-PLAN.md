---
phase: 06-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ticketing/package.json
  - ticketing/package-lock.json
  - ticketing/src/db/knex.js
  - ticketing/index.js
  - ticketing/.env.example
autonomous: false
requirements:
  - SEC-04
must_haves:
  truths:
    - "App boots on Railway connected to managed Postgres with no manual schema step"
    - "knex.js selects DATABASE_PRIVATE_URL over DATABASE_URL, DATABASE_URL over SQLite"
    - "SIGTERM triggers graceful shutdown — server.close + db.destroy + 10s hard exit"
    - "express-rate-limit is installed and available to require() in scan.js"
  artifacts:
    - path: "ticketing/package.json"
      provides: "express-rate-limit dependency"
      contains: "express-rate-limit"
    - path: "ticketing/src/db/knex.js"
      provides: "three-branch URL priority config"
      contains: "DATABASE_PRIVATE_URL"
    - path: "ticketing/index.js"
      provides: "SIGTERM handler and server reference"
      contains: "process.on('SIGTERM'"
    - path: "ticketing/.env.example"
      provides: "DATABASE_PRIVATE_URL documentation"
      contains: "DATABASE_PRIVATE_URL"
  key_links:
    - from: "ticketing/index.js"
      to: "ticketing/src/db/knex.js"
      via: "db.destroy() in SIGTERM handler"
      pattern: "db\\.destroy"
    - from: "ticketing/index.js"
      to: "http.Server"
      via: "const server = app.listen()"
      pattern: "const server = app\\.listen"

user_setup: []
---

<objective>
Install express-rate-limit, extend knex.js to prefer DATABASE_PRIVATE_URL (Railway private
network), capture app.listen() return value in index.js, add SIGTERM graceful shutdown handler,
and document DATABASE_PRIVATE_URL in .env.example.

Purpose: Establishes the production infrastructure plumbing that Railway requires — correct
Postgres connection routing and a clean process shutdown path. express-rate-limit must be
installed here so Plan 06-02 can require() it in scan.js.

Output: Modified package.json (+ lock file), knex.js with three-branch URL priority, index.js
with server reference capture + SIGTERM handler, .env.example with DATABASE_PRIVATE_URL block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-production-hardening/06-CONTEXT.md
@.planning/phases/06-production-hardening/06-RESEARCH.md
@.planning/phases/06-production-hardening/06-PATTERNS.md

<interfaces>
<!-- Current state of files being modified. Read before touching anything. -->

From ticketing/src/db/knex.js (current — two-branch):
The file is 31 lines. Current top-level ternary checks DATABASE_URL only; no DATABASE_PRIVATE_URL branch.
Key structure:
  const db = knex(
    process.env.DATABASE_URL
      ? { client: 'pg', connection: { connectionString: ..., ssl: { rejectUnauthorized: false } }, migrations: {...} }
      : { client: 'better-sqlite3', connection: { filename: ... }, useNullAsDefault: true, migrations: {...} }
  );

From ticketing/index.js (current — discards app.listen() return):
Line 52–65:
  async function start() {
    try {
      await db.migrate.latest();
      console.log('Migrations complete');
      app.listen(PORT, () => {           // <-- return value discarded (D-07 bug)
        console.log(`Listening on port ${PORT}`);
      });
    } catch (err) {
      console.error('Startup failed:', err);
      process.exit(1);
    }
  }
  start();                               // line 65 — SIGTERM handler goes BEFORE this line

From ticketing/.env.example (lines 1–10):
  # Copy to .env for local development. ...
  PORT=3000
  # Leave unset in development to use SQLite (dev.sqlite)
  # Set to your Railway Postgres URL in production
  # DATABASE_URL=postgresql://user:password@host:5432/dbname
  (DATABASE_PRIVATE_URL block to be inserted immediately after DATABASE_URL block)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify express-rate-limit package legitimacy and install</name>
  <read_first>
    ticketing/package.json — confirm express-rate-limit is absent from dependencies before installing
  </read_first>
  <what-built>
    The research phase verified express-rate-limit at npmjs.com/package/express-rate-limit:
    - Package age: ~11 years (created 2014-12-11)
    - Last published: 2026-05-14 (version 8.5.2)
    - Source repo: github.com/express-rate-limit/express-rate-limit (org-scoped, active)
    - No postinstall script (confirmed via npm view express-rate-limit scripts.postinstall)
    - Tagged [ASSUMED] because slopcheck was unavailable at research time

    After you approve, run from the ticketing/ directory:
      cd ticketing && npm install express-rate-limit@^8.5.2
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/express-rate-limit — confirm the package exists,
       the publisher is nfriedly (Nathan Friedly, longtime maintainer), and the version shown
       is 8.x (not a suspicious fork or typosquat).
    2. Visit https://github.com/express-rate-limit/express-rate-limit — confirm the repo is
       active, owned by the express-rate-limit org (not an individual with no history), and
       has recent commits.
    3. Confirm the npm page does not show a postinstall script.

    If anything looks suspicious, type "REJECT" and do not install. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with npm install, or "REJECT" to stop and report</resume-signal>
  <acceptance_criteria>
    - Human has visually confirmed the package on npmjs.com and GitHub
    - Response is "approved" (not REJECT)
    - After install: ticketing/package.json contains "express-rate-limit" in dependencies
    - After install: node -e "require('express-rate-limit')" exits 0 when run from ticketing/
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend knex.js to three-branch DATABASE_PRIVATE_URL priority</name>
  <files>ticketing/src/db/knex.js</files>
  <read_first>
    ticketing/src/db/knex.js — read full file (31 lines) to understand exact current ternary structure before editing
    .planning/phases/06-production-hardening/06-PATTERNS.md — "ticketing/src/db/knex.js" section for exact target shape
    .planning/phases/06-production-hardening/06-RESEARCH.md — Pattern 3 for three-branch ternary and ssl:false rationale
  </read_first>
  <behavior>
    - When DATABASE_PRIVATE_URL is set: knex uses client pg, connectionString from DATABASE_PRIVATE_URL, ssl: false
    - When DATABASE_URL is set (and DATABASE_PRIVATE_URL is not): knex uses client pg, connectionString from DATABASE_URL, ssl: { rejectUnauthorized: false } (unchanged from current)
    - When neither is set: knex uses client better-sqlite3, filename dev.sqlite (unchanged from current)
  </behavior>
  <action>
    Replace the existing two-branch ternary in knex.js with a three-branch ternary. Prepend a
    new first branch that checks process.env.DATABASE_PRIVATE_URL. The new first branch config
    object is: client 'pg', connection with connectionString from DATABASE_PRIVATE_URL and
    ssl: false (per D-02 — Railway private network uses Wireguard, no TLS needed), and
    migrations directory unchanged. The existing DATABASE_URL branch and SQLite branch are
    unchanged — preserve all existing inline comments verbatim. Add inline comment on ssl: false
    line: "// Railway private network uses Wireguard — no TLS needed (D-02)". Preserve 2-space
    top-level indent and 4-space-inside-object-literal indent pattern exactly.
  </action>
  <verify>
    <automated>cd /Users/jkendall/git/personal/powderrhythm/ticketing && node -e "
      const path = require('path');
      process.env.DATABASE_PRIVATE_URL = 'postgres://x:x@postgres.railway.internal:5432/railway';
      delete process.env.DATABASE_URL;
      const k1 = require('./src/db/knex');
      const cfg1 = k1.client.config;
      console.assert(cfg1.client === 'pg', 'PRIVATE_URL: client should be pg');
      console.assert(cfg1.connection.ssl === false, 'PRIVATE_URL: ssl should be false');
      console.log('PRIVATE_URL branch: OK');
    " && node -e "
      delete require.cache[require.resolve('./src/db/knex')];
      delete process.env.DATABASE_PRIVATE_URL;
      process.env.DATABASE_URL = 'postgres://x:x@host:5432/db';
      const k2 = require('./src/db/knex');
      const cfg2 = k2.client.config;
      console.assert(cfg2.client === 'pg', 'DATABASE_URL: client should be pg');
      console.assert(cfg2.connection.ssl != null, 'DATABASE_URL: ssl should be set');
      console.log('DATABASE_URL branch: OK');
    " && node -e "
      delete require.cache[require.resolve('./src/db/knex')];
      delete process.env.DATABASE_PRIVATE_URL;
      delete process.env.DATABASE_URL;
      const k3 = require('./src/db/knex');
      const cfg3 = k3.client.config;
      console.assert(cfg3.client === 'better-sqlite3', 'SQLite branch: client should be better-sqlite3');
      console.log('SQLite branch: OK');
    "
    </automated>
  </verify>
  <acceptance_criteria>
    - ticketing/src/db/knex.js contains the string "DATABASE_PRIVATE_URL"
    - ticketing/src/db/knex.js contains "ssl: false" on the PRIVATE_URL branch
    - ticketing/src/db/knex.js still contains "ssl: { rejectUnauthorized: false }" on the DATABASE_URL branch
    - The three node -e assertions above all print "OK" with exit code 0
    - Existing test suite still passes: node --test test/scan-api.test.js from ticketing/ exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Capture server reference and add SIGTERM handler to index.js; update .env.example</name>
  <files>ticketing/index.js, ticketing/.env.example</files>
  <read_first>
    ticketing/index.js — read full file (66 lines) to see exact start() function and where start() call is on line 65
    .planning/phases/06-production-hardening/06-PATTERNS.md — "ticketing/index.js" section for exact SIGTERM handler shape
    .planning/phases/06-production-hardening/06-RESEARCH.md — Pattern 2 for SIGTERM handler, SIGINT handler recommendation
    ticketing/.env.example — read full file (32 lines) to find DATABASE_URL block insertion point
    .planning/phases/06-production-hardening/06-PATTERNS.md — "ticketing/.env.example" section for insertion point and comment style
  </read_first>
  <action>
    index.js — two changes:

    1. Capture app.listen() return value (D-07): change line 56 from
       "app.listen(PORT, () => {" to "const server = app.listen(PORT, () => {".
       The callback body is unchanged.

    2. Add a named shutdown() function and SIGTERM + SIGINT handlers at module level,
       inserted between the closing brace of start() (line 63) and the start() call (line 65).
       The shutdown() function: starts a 10-second hard-exit timer via setTimeout with
       process.exit(0) (per D-06), calls server.close(), inside the server.close() callback
       calls clearTimeout(timer) then db.destroy().then(() => process.exit(0)).catch(() =>
       process.exit(0)). Register shutdown for both process.on('SIGTERM') and
       process.on('SIGINT') (per research open question 3 — Claude's Discretion). Log
       "SIGTERM received — shutting down gracefully" or "SIGINT received — shutting down
       gracefully" respectively. Use console.error for the timeout message:
       "Graceful shutdown timed out — forcing exit". Preserve 'use strict' and existing
       require block — no new requires needed (db is already required on line 7).

    .env.example — insert DATABASE_PRIVATE_URL block immediately after the DATABASE_URL
    comment block (after line 7 "# DATABASE_URL=postgresql://..."). Block content:
    Three comment lines followed by the commented-out example value. First comment:
    "# Railway private network URL — only available inside Railway containers."
    Second comment: "# Takes priority over DATABASE_URL; uses ssl:false (Wireguard encrypts
    private network)." Third comment: "# Never set this locally — Railway DNS is not
    resolvable outside Railway." Example value line:
    "# DATABASE_PRIVATE_URL=postgresql://user:password@postgres.railway.internal:5432/railway"
    Preserve the blank line after the DATABASE_URL block before the next section.
  </action>
  <verify>
    <automated>
      cd /Users/jkendall/git/personal/powderrhythm/ticketing && node -e "
        const src = require('fs').readFileSync('index.js', 'utf8');
        console.assert(src.includes('const server = app.listen'), 'server reference captured');
        console.assert(src.includes(\"process.on('SIGTERM'\"), 'SIGTERM handler present');
        console.assert(src.includes(\"process.on('SIGINT'\"), 'SIGINT handler present');
        console.assert(src.includes('server.close'), 'server.close present');
        console.assert(src.includes('db.destroy'), 'db.destroy present');
        console.assert(src.includes('clearTimeout'), 'clearTimeout present');
        console.log('index.js assertions: OK');
      " && node -e "
        const src = require('fs').readFileSync('.env.example', 'utf8');
        console.assert(src.includes('DATABASE_PRIVATE_URL'), '.env.example has PRIVATE_URL');
        console.assert(src.includes('postgres.railway.internal'), '.env.example has internal hostname');
        console.log('.env.example assertions: OK');
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - ticketing/index.js contains "const server = app.listen"
    - ticketing/index.js contains "process.on('SIGTERM'" and "process.on('SIGINT'"
    - ticketing/index.js contains "server.close", "clearTimeout", "db.destroy"
    - ticketing/.env.example contains "DATABASE_PRIVATE_URL" and "postgres.railway.internal"
    - node --test test/scan-api.test.js from ticketing/ still exits 0 (no regressions)
    - node index.js starts and responds to GET /health (smoke test — Ctrl+C to stop)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → knex.js | DATABASE_PRIVATE_URL and DATABASE_URL are injected from Railway env; incorrect values cause startup failure, not security breach |
| process signals → index.js | SIGTERM/SIGINT come from Railway/OS; malicious signal injection is a host-level concern, not application scope |
| npm registry → ticketing/node_modules | express-rate-limit install fetches from npmjs.com CDN; supply chain risk if package is compromised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | Cookie `secure` flag | mitigate | NODE_ENV=production must be set in Railway env (D-09); scan.js line 35 already gates on this — no code change needed, ops task covers it |
| T-06-02 | Denial of Service | SIGTERM keep-alive hang | mitigate | 10-second hard-exit timer in shutdown() ensures process.exit(0) fires even if keep-alive connections don't drain (D-06) |
| T-06-03 | Information Disclosure | DATABASE_PRIVATE_URL in logs | accept | URL is logged only on startup error (not on success); Railway's internal DNS is not accessible outside the private network; risk is low |
| T-06-04 | Tampering | npm/pip/cargo installs | mitigate | express-rate-limit human-verify checkpoint (Task 1) gates install; package provenance verified via npmjs.com + GitHub before approval |
| T-06-SC | Tampering | npm install express-rate-limit | mitigate | blocking human checkpoint in Task 1 requires visual verification of npmjs.com and GitHub before install proceeds; [ASSUMED] per slopcheck unavailability |
</threat_model>

<verification>
After all tasks complete:

1. node --test test/scan-api.test.js from ticketing/ passes all 11 existing tests
2. ticketing/src/db/knex.js contains "DATABASE_PRIVATE_URL" and "ssl: false"
3. ticketing/index.js contains "const server = app.listen" and "process.on('SIGTERM'"
4. ticketing/.env.example contains "DATABASE_PRIVATE_URL"
5. ticketing/package.json dependencies contains "express-rate-limit"
6. node -e "require('express-rate-limit')" exits 0 from ticketing/
</verification>

<success_criteria>
1. express-rate-limit is installed and require()-able — Plan 06-02 can proceed
2. knex.js prefers DATABASE_PRIVATE_URL (ssl:false) over DATABASE_URL (ssl:{rejectUnauthorized:false}) over SQLite
3. index.js captures server reference and handles SIGTERM + SIGINT gracefully with 10s hard exit
4. .env.example documents DATABASE_PRIVATE_URL with the correct Railway internal hostname example
5. All 11 existing scan tests still pass
</success_criteria>

<output>
Create `.planning/phases/06-production-hardening/06-01-SUMMARY.md` when done
</output>
